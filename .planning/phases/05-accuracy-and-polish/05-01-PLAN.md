---
phase: 05-accuracy-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/schemas/stock.ts
  - src/actions/stock.ts
  - src/components/stock/waste-form.tsx
  - src/app/(dashboard)/stock/waste/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - STCK-03

must_haves:
  truths:
    - "User can select an item, enter waste quantity in user-friendly units, pick a reason code, and submit"
    - "Stock level decreases by the waste amount after submission"
    - "A WASTE ledger entry appears in transaction history with reason, user, and timestamp"
    - "Waste page is accessible from the sidebar under the Stock section"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "WASTE_REASON_CODES constant array"
      contains: "WASTE_REASON_CODES"
    - path: "src/schemas/stock.ts"
      provides: "wasteSchema Zod validation"
      contains: "wasteSchema"
    - path: "src/actions/stock.ts"
      provides: "recordWaste server action with atomic ledger"
      contains: "recordWaste"
    - path: "src/components/stock/waste-form.tsx"
      provides: "Client-side waste recording form"
      contains: "WasteForm"
    - path: "src/app/(dashboard)/stock/waste/page.tsx"
      provides: "Waste recording page (server component)"
      contains: "WastePage"
    - path: "src/components/layout/sidebar.tsx"
      provides: "Waste nav item in Stock section"
      contains: "/stock/waste"
  key_links:
    - from: "src/components/stock/waste-form.tsx"
      to: "src/actions/stock.ts"
      via: "recordWaste server action call on form submit"
      pattern: "recordWaste"
    - from: "src/actions/stock.ts"
      to: "prisma.$transaction"
      via: "atomic ledger write + stockQty decrement"
      pattern: "prisma\\.\\$transaction"
    - from: "src/app/(dashboard)/stock/waste/page.tsx"
      to: "src/components/stock/waste-form.tsx"
      via: "server component loads items, passes to client form"
      pattern: "WasteForm.*items"
---

<objective>
Implement waste/spoilage recording so users can log material losses with reason codes, automatically adjusting stock levels through the atomic ledger pattern.

Purpose: Enables accurate stock tracking by accounting for material waste and spoilage -- without this, system stock drifts from reality over time.
Output: Waste recording form, server action, reason codes, sidebar navigation entry.
</objective>

<execution_context>
@/Users/envicomm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/envicomm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accuracy-and-polish/05-RESEARCH.md

@src/actions/stock.ts
@src/schemas/stock.ts
@src/lib/constants.ts
@src/components/stock/receiving-form.tsx
@src/components/layout/sidebar.tsx
@src/app/(dashboard)/stock/history/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add waste reason codes, Zod schema, and recordWaste server action</name>
  <files>
    src/lib/constants.ts
    src/schemas/stock.ts
    src/actions/stock.ts
  </files>
  <action>
1. In `src/lib/constants.ts`, add `WASTE_REASON_CODES` constant array after the existing constants:
   ```
   export const WASTE_REASON_CODES = [
     { value: "EXPIRED", label: "Expired" },
     { value: "SPOILED", label: "Spoiled / Contaminated" },
     { value: "DAMAGED", label: "Damaged" },
     { value: "QUALITY", label: "Quality Issue" },
     { value: "SPILL", label: "Spilled / Dropped" },
     { value: "OTHER", label: "Other" },
   ] as const;
   ```

2. In `src/schemas/stock.ts`, add `wasteSchema` and `WasteFormData` type below the existing `receivingSchema`:
   - `itemId`: z.number().int().positive("Select an item")
   - `quantity`: z.number().positive("Quantity must be positive") -- user enters in grams (weight items) or pieces (packaging)
   - `reasonCode`: z.string().min(1, "Select a reason")
   - `notes`: z.string().max(500).optional()
   Export the type: `export type WasteFormData = z.infer<typeof wasteSchema>;`

3. In `src/actions/stock.ts`, add `recordWaste` server action following the exact `receiveStock` pattern:
   - Import `wasteSchema` from schemas/stock and `gramsToMg` from lib/utils
   - Call `requireRole("staff")` to get user
   - Parse with `wasteSchema.safeParse(rawData)`
   - Load item with `prisma.item.findUnique` and check it exists + not deleted
   - Convert quantity to storage units: PACKAGING items stay as pieces; all others use `gramsToMg(data.quantity)` to convert grams to milligrams
   - Generate referenceId: `WST-${Date.now()}`
   - ATOMIC transaction with `prisma.$transaction([...])`:
     a. `prisma.inventoryTransaction.create` with type "WASTE", quantity as NEGATIVE (waste removes stock), notes as `[${data.reasonCode}] ${data.notes || ""}`.trim(), createdBy as user.id
     b. `prisma.item.update` with `stockQty: { decrement: wasteQty }` where wasteQty is the positive storage-unit value
   - Call `revalidatePath` for: "/stock/waste", "/stock/history", "/items", "/"
   - Return `{ success: true }` on success, `{ error: string }` on failure
   - Wrap in try/catch like receiveStock

CRITICAL: Waste ledger quantity MUST be negative (removes stock). The decrement value must be positive. Do NOT copy the positive quantity from receiveStock without flipping the sign.
  </action>
  <verify>
    <automated>cd /Users/envicomm1/Documents/GitHub/inventory-management && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify wasteSchema exports, recordWaste function exists, WASTE_REASON_CODES exported</manual>
  </verify>
  <done>wasteSchema validates waste form input; recordWaste atomically writes WASTE ledger entry with negative quantity and decrements stockQty; WASTE_REASON_CODES array available for form dropdown</done>
</task>

<task type="auto">
  <name>Task 2: Build waste recording form, page, and sidebar navigation</name>
  <files>
    src/components/stock/waste-form.tsx
    src/app/(dashboard)/stock/waste/page.tsx
    src/components/layout/sidebar.tsx
  </files>
  <action>
1. Create `src/components/stock/waste-form.tsx` as a client component following the `receiving-form.tsx` pattern exactly:
   - "use client" directive
   - Props: `items` array with `{ id, name, sku, type }` shape (no cartonSize/unitWeightMg needed since user enters in display units)
   - Use `useForm<WasteFormData>` with `standardSchemaResolver(wasteSchema)` -- import from `@hookform/resolvers/standard-schema`
   - Default values: itemId=0, quantity=0, reasonCode="", notes=""
   - Form fields:
     a. Item selector: native HTML `<select>` with same styling classes as receiving-form (matching project pattern)
     b. Quantity: `<Input type="number" step="0.01" min="0.01">` with label that dynamically shows unit based on selected item type -- "Quantity (grams)" for weight items, "Quantity (pieces)" for packaging. Use `watch("itemId")` + useMemo to find selected item and determine unit label.
     c. Reason code: native HTML `<select>` populated from WASTE_REASON_CODES imported from `@/lib/constants`
     d. Notes: `<Textarea>` with placeholder "Additional details (optional)"
   - Submit handler: call `recordWaste(payload)` with `itemId: Number(formData.itemId), quantity: Number(formData.quantity), reasonCode: formData.reasonCode, notes: formData.notes || undefined`
   - Success/error status display matching receiving-form pattern (green/red banner)
   - Reset form on success
   - Submit button text: "Record Waste" / "Recording..." when submitting

2. Create `src/app/(dashboard)/stock/waste/page.tsx` as a server component:
   - Import prisma, load active items: `prisma.item.findMany({ where: { deletedAt: null }, select: { id, name, sku, type }, orderBy: { name: "asc" } })`
   - Page header: h1 "Record Waste" with description "Log spoilage, expiration, or damaged stock. Stock levels will be adjusted automatically."
   - Render `<WasteForm items={items} />`

3. Update `src/components/layout/sidebar.tsx`:
   - Add `Trash2` to the lucide-react import (for waste icon)
   - Add a new nav item inside the Stock section, between "Receiving" and "History":
     ```
     { name: "Waste", href: "/stock/waste", icon: Trash2, roles: ["admin", "staff"] },
     ```
   - Keep existing items in their current order (Receiving first, then Waste, then History)
  </action>
  <verify>
    <automated>cd /Users/envicomm1/Documents/GitHub/inventory-management && npx tsc --noEmit 2>&1 | head -30 && echo "---BUILD CHECK---" && npx next build 2>&1 | tail -20</automated>
    <manual>Visit /stock/waste -- form should display with item selector, quantity field, reason dropdown, notes textarea, and submit button</manual>
  </verify>
  <done>Waste recording page accessible at /stock/waste with functional form; sidebar shows Waste link under Stock section; form validates input and calls recordWaste server action; success resets form and shows confirmation</done>
</task>

</tasks>

<verification>
1. TypeScript compiles with no errors: `npx tsc --noEmit`
2. Next.js builds successfully: `npx next build`
3. /stock/waste page renders without runtime errors
4. Sidebar shows Stock > Receiving, Waste, History
5. WASTE_REASON_CODES has 6 reason options
6. wasteSchema validates: positive quantity, non-empty reasonCode, valid itemId
7. recordWaste creates negative ledger entry and decrements stockQty atomically
</verification>

<success_criteria>
- User can navigate to /stock/waste from the sidebar
- User can select an item, enter quantity in grams or pieces (based on type), select a reason code, and submit
- After submission, a WASTE transaction appears in /stock/history with orange badge, negative quantity, reason in notes
- The item's stock level on /items and dashboard decreases by the waste amount
- Form resets after successful submission with a green success message
</success_criteria>

<output>
After completion, create `.planning/phases/05-accuracy-and-polish/05-01-SUMMARY.md`
</output>
