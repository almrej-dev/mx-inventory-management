---
phase: 03-sales-and-auto-deduction
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/sales/manual-entry-form.tsx
  - src/components/sales/sales-history-table.tsx
  - src/components/sales/sales-columns.tsx
  - src/app/(dashboard)/sales/manual/page.tsx
  - src/app/(dashboard)/sales/history/page.tsx
  - src/app/(dashboard)/sales/[uploadId]/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - SALE-02
  - SALE-04

must_haves:
  truths:
    - "User can manually enter individual sales lines by selecting a finished product and quantity"
    - "Manual sales are processed with the same BOM deduction as uploaded sales"
    - "User can view a list of all sales uploads/entries with date, source, status, and line count"
    - "User can click into a sales upload to see individual sales lines with product details"
    - "Sales section appears in sidebar navigation for all roles"
  artifacts:
    - path: "src/components/sales/manual-entry-form.tsx"
      provides: "Form with product selector (finished items with recipes), quantity, date, optional price and notes"
      min_lines: 60
    - path: "src/components/sales/sales-history-table.tsx"
      provides: "TanStack Table listing sales uploads with source badge, date, status, line count"
      min_lines: 50
    - path: "src/app/(dashboard)/sales/manual/page.tsx"
      provides: "Manual sales entry page"
      min_lines: 15
    - path: "src/app/(dashboard)/sales/history/page.tsx"
      provides: "Sales history listing page"
      min_lines: 20
    - path: "src/app/(dashboard)/sales/[uploadId]/page.tsx"
      provides: "Sales upload detail page showing individual sales lines"
      min_lines: 30
    - path: "src/components/layout/sidebar.tsx"
      provides: "Updated sidebar with Sales section (Upload, Manual Entry, History)"
      contains: "Sales"
  key_links:
    - from: "src/components/sales/manual-entry-form.tsx"
      to: "src/actions/sales.ts"
      via: "processSalesLines call with source='manual'"
      pattern: "processSalesLines"
    - from: "src/components/sales/sales-history-table.tsx"
      to: "src/actions/sales.ts"
      via: "getSalesUploads for data loading"
      pattern: "getSalesUploads"
    - from: "src/app/(dashboard)/sales/[uploadId]/page.tsx"
      to: "src/actions/sales.ts"
      via: "getSalesUploadDetail for detail view"
      pattern: "getSalesUploadDetail"
    - from: "src/components/layout/sidebar.tsx"
      to: "/sales/*"
      via: "Navigation links for Sales section"
      pattern: "sales"
---

<objective>
Build the manual sales entry form, sales history pages, and sidebar navigation for the sales module.

Purpose: This provides the manual entry alternative (SALE-02) for when POS exports are unavailable, and the sales history view (SALE-04) for tracking processed uploads. Both features reuse the processSalesLines server action from Plan 01.

Output: Manual entry form page, sales history list page, upload detail page, and updated sidebar with Sales section.
</objective>

<execution_context>
@/Users/envicomm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/envicomm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sales-and-auto-deduction/03-RESEARCH.md
@.planning/phases/03-sales-and-auto-deduction/03-01-SUMMARY.md

@src/actions/sales.ts
@src/schemas/sales.ts
@src/components/layout/sidebar.tsx
@src/components/stock/receiving-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manual sales entry form and page</name>
  <files>
    src/components/sales/manual-entry-form.tsx
    src/app/(dashboard)/sales/manual/page.tsx
  </files>
  <action>
1. Create src/components/sales/manual-entry-form.tsx ("use client"):
   - Follow the pattern from src/components/stock/receiving-form.tsx (react-hook-form + standardSchemaResolver + useTransition for submit)
   - Form fields:
     - saleDate: date input, default to today (use z.string() pattern like receivedDate in stock schema)
     - Dynamic line rows (useFieldArray pattern from recipe form):
       - Each row: product selector (native HTML select, filtered to finished items with recipes from getFinishedItems()), quantity (number input, min 1), unit price in pesos (optional number input)
       - "Add Line" button to add another row
       - "Remove" button on each row (only when more than 1 row exists)
     - notes: optional textarea, max 500 chars
   - On mount, fetch finished items via getFinishedItems() server action
   - On submit:
     - Build processSalesLines input: { source: "manual", saleDate, notes, lines: rows mapped to { itemId, productName (from selected item's name), quantity, unitPriceCentavos (pesos * 100 if provided) } }
     - Call processSalesLines server action
     - Use useTransition for pending state (button shows "Processing..." when pending)
     - On success, show success message and redirect to /sales/history
     - On error, show error message at form level
   - Use Zod schema for form validation:
     - Create a manualEntrySchema in the component or in src/schemas/sales.ts:
       - saleDate: z.string().min(1)
       - lines: z.array(z.object({ itemId: z.number().positive(), quantity: z.number().int().positive(), unitPricePesos: z.number().nonnegative().optional() })).min(1)
       - notes: z.string().max(500).optional()

2. Create src/app/(dashboard)/sales/manual/page.tsx:
   - Server component page shell
   - Page title: "Manual Sales Entry"
   - Description text: "Record individual sales when no POS export is available."
   - Render ManualEntryForm component
  </action>
  <verify>
    <automated>cd /Users/envicomm1/Documents/GitHub/inventory-management && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify manual entry form compiles, has product selector and dynamic rows</manual>
  </verify>
  <done>
    - Manual entry form shows product selector filtered to finished items with recipes
    - Dynamic line rows with add/remove functionality
    - Form submits via processSalesLines with source="manual"
    - Redirect to /sales/history on success
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sales history pages, upload detail page, and update sidebar navigation</name>
  <files>
    src/components/sales/sales-history-table.tsx
    src/components/sales/sales-columns.tsx
    src/app/(dashboard)/sales/history/page.tsx
    src/app/(dashboard)/sales/[uploadId]/page.tsx
    src/components/layout/sidebar.tsx
  </files>
  <action>
1. Create src/components/sales/sales-columns.tsx:
   - TanStack Table column definitions for sales history table (follow pattern from items table columns)
   - Columns:
     - Date: format saleDate with date-fns format(date, "MMM d, yyyy")
     - Source: badge showing "Upload" (blue) or "Manual" (green) based on source field
     - File Name: show fileName or "Manual Entry" if null
     - Lines: totalLines count
     - Status: badge -- "completed" green, "failed" red, "pending" yellow
     - Actions: Link to /sales/{uploadId} detail page

2. Create src/components/sales/sales-history-table.tsx ("use client"):
   - Follow TanStack Table pattern from items table (useReactTable, flexRender)
   - Receives uploads data as prop (loaded by server component parent)
   - Simple table with sorting by date (default desc) and pagination
   - No search/filter needed for v1 (keep simple)
   - Empty state: "No sales recorded yet. Upload a file or enter sales manually."

3. Create src/app/(dashboard)/sales/history/page.tsx:
   - Server component
   - Call getSalesUploads() to load data
   - Page title: "Sales History"
   - Render SalesHistoryTable with uploads data
   - Add action buttons in header: "Upload File" link to /sales/upload, "Manual Entry" link to /sales/manual (wrapped in RoleGate for staff+ roles)

4. Create src/app/(dashboard)/sales/[uploadId]/page.tsx:
   - Server component
   - Parse uploadId from params (parseInt)
   - Call getSalesUploadDetail(uploadId) to load data
   - If not found, show "Sales record not found" with back link
   - Page title: "Sales Details - {fileName or 'Manual Entry'} - {formatted saleDate}"
   - Show upload metadata: source, date, status, total lines, notes
   - Show sales lines in a simple HTML table:
     - Columns: Product Name (from CSV/manual), Matched Item (item.name + item.sku), Quantity, Unit Price (formatted from centavos to pesos), Deducted (checkmark or X)
   - Back link to /sales/history

5. Update src/components/layout/sidebar.tsx:
   - Add a "Sales" NavSection after the "Recipes" section, before "Stock"
   - Import FileUp, PenLine, and ReceiptText icons from lucide-react (or similar -- Upload, PenSquare, Receipt)
   - Sales section items:
     - "Upload Sales" href="/sales/upload" roles=["admin","staff"] icon=FileUp
     - "Manual Entry" href="/sales/manual" roles=["admin","staff"] icon=PenLine
     - "Sales History" href="/sales/history" roles=["admin","staff","viewer"] icon=ReceiptText (or History)
  </action>
  <verify>
    <automated>cd /Users/envicomm1/Documents/GitHub/inventory-management && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify sidebar shows Sales section, history page loads, detail page shows lines</manual>
  </verify>
  <done>
    - Sales history table displays uploads with source badge, date, status, line count
    - Detail page shows individual sales lines with matched product info
    - Sidebar has Sales section with Upload, Manual Entry, and History links
    - Staff+ can access upload and manual entry, viewer+ can access history
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. Manual entry form renders with product selector and dynamic line rows
2. Manual entry submits via processSalesLines with source="manual"
3. Sales history table displays uploads with correct columns and badges
4. Upload detail page shows individual sales lines
5. Sidebar shows Sales section with three navigation items
6. Role-based visibility: staff+ for upload/manual, viewer+ for history
7. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- User can manually enter sales with product selection and quantity
- User can view list of all sales uploads/entries
- User can click into any sales record to see line details
- Sidebar navigation includes Sales section with all three pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-sales-and-auto-deduction/03-03-SUMMARY.md`
</output>
