---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - ITEM-01
  - ITEM-02
  - ITEM-03
  - ITEM-04
  - ITEM-05
  - ITEM-06
  - STCK-01
  - STCK-05

must_haves:
  truths:
    - "User can complete the full flow: login -> create item -> receive stock -> verify stock level"
    - "Role enforcement works: viewer cannot create items, staff cannot manage users"
    - "Stock levels update immediately after receiving"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 foundation by running the Supabase SQL migration, pushing the Prisma schema, seeding the admin user, and testing the full end-to-end flow.

Purpose: Phase 1 has external setup dependencies (Supabase project, SQL migration, environment variables) that cannot be automated. This plan ensures everything is wired together and working before moving to Phase 2.

Output: A fully functional Phase 1 application verified by the user.
</objective>

<execution_context>
@/Users/envicomm1/.claude/get-shit-done/workflows/execute-plan.md
@/Users/envicomm1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Prisma migration, verify build, and prepare setup instructions</name>
  <files></files>
  <action>
    1. Verify the user has configured `.env.local` with all required Supabase credentials by checking the file exists. If not, output clear instructions referencing `.env.local.example`.

    2. Run `pnpm prisma db push` to push the Prisma schema to the Supabase database. If this fails due to missing env vars, halt and provide instructions.

    3. Run `pnpm prisma generate` to generate the Prisma client.

    4. Verify `pnpm build` succeeds with the generated Prisma client.

    5. Prepare a checklist for the user covering the manual Supabase setup steps:
       - Run the SQL migration from `supabase/migrations/00001_rbac_setup.sql` in the Supabase SQL Editor
       - Enable the custom access token hook in Supabase Dashboard -> Authentication -> Hooks
       - Run `pnpm prisma db seed` to create the initial admin user
       - Start the dev server with `pnpm dev`

    NOTE: If any step requires user interaction (env vars not set, Supabase not configured), create clear terminal output explaining exactly what to do.
  </action>
  <verify>
    <automated>cd /Users/envicomm1/Documents/GitHub/inventory-management && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>Prisma schema pushed to database, client generated, build succeeds, setup checklist prepared for user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies complete Phase 1 flow</name>
  <files></files>
  <action>
    Present the following verification checklist to the user and wait for their confirmation.

    Prerequisites (complete these first if not done):
    1. Copy `.env.local.example` to `.env.local` and fill in your Supabase credentials
    2. Run the SQL from `supabase/migrations/00001_rbac_setup.sql` in Supabase SQL Editor
    3. Enable the custom access token hook in Supabase Dashboard -> Authentication -> Hooks -> Customize Access Token
    4. Run `pnpm prisma db push` then `pnpm prisma db seed`
    5. Start the dev server: `pnpm dev`

    Verification steps:
    1. AUTH: Visit http://localhost:3000 -- should redirect to /login
    2. AUTH: Log in with admin credentials from seed (admin@example.com / admin123456) -- should redirect to dashboard
    3. AUTH: Navigate to /users -- should show user management page
    4. AUTH: Create a new "staff" user and a "viewer" user
    5. AUTH: Sign out, log in as the viewer -- verify /users is not accessible
    6. AUTH: Sign out, log in as staff -- verify /users is not accessible but /items and /stock/receiving are

    7. ITEMS: Navigate to /items -- should show empty item list with "Add Item" button
    8. ITEMS: Click "Add Item" -- create a raw material (e.g., "Fresh Cream", RM-DC-001, weight 1000g, carton size 8, cost PHP 150)
    9. ITEMS: Create a packaging item (e.g., "Medium Cup", PK-CP-001, carton size 50, cost PHP 5)
    10. ITEMS: Verify items appear in the list with correct display values (grams, PHP)
    11. ITEMS: Edit an item -- verify pre-populated values are correct
    12. ITEMS: Delete an item -- verify it disappears from the list (soft-delete)
    13. ITEMS: Test search by name and filter by type
    14. ITEMS: Visit /items/sku-legend -- verify format guide is displayed

    15. STOCK: Navigate to /stock/receiving
    16. STOCK: Select an item, enter 2 cartons, verify conversion display shows correct grams
    17. STOCK: Submit the receiving -- verify success message
    18. STOCK: Check /items -- verify stock level updated
    19. STOCK: Check /stock/history -- verify transaction entry appears with correct data

    Expected outcomes:
    - All pages load without errors
    - Role-based access works correctly (admin > staff > viewer)
    - Item CRUD works with proper unit conversion display
    - Stock receiving atomically updates both ledger and stock level
    - Transaction history shows all movements
  </action>
  <verify>User confirms "approved" or describes issues found</verify>
  <done>User has verified all Phase 1 features work correctly end-to-end: authentication with RBAC, item CRUD with unit conversion, stock receiving with atomic ledger updates, and transaction history.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when the user has verified:
1. Authentication and role-based access control works for all three roles
2. Item CRUD with all four types, search, filter, and soft-delete works
3. Stock receiving creates ledger entries and updates stock levels atomically
4. Transaction history displays all stock movements
5. Unit conversion (mg/centavos internally, grams/pesos in UI) works correctly throughout
</verification>

<success_criteria>
- User can complete the full flow: login -> manage items -> receive stock -> view history
- Role enforcement works for admin, staff, and viewer
- All five success criteria from ROADMAP Phase 1 are verified by user observation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
